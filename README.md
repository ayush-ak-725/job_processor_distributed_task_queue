# job_processor_distributed_task_queue
A small-scale distributed job queue and worker system with authenticated users submit tasks, process them with worker nodes, and track progress in real time through a simple dashboard having a reliable, durable, and observable system.
